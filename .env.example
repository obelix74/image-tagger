# Image Tagger Configuration
# Copy this file to .env and update the values

# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# =============================================================================
# AI PROVIDER CONFIGURATION
# =============================================================================

# AI Provider Selection ('gemini' or 'ollama')
# Default: gemini
AI_PROVIDER=gemini

# Google Gemini AI Configuration (when AI_PROVIDER=gemini)
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Ollama Configuration (when AI_PROVIDER=ollama)
# Ollama server base URL (default: http://localhost:11434)
OLLAMA_BASE_URL=http://localhost:11434
# Ollama model for vision tasks (default: llava:latest)
OLLAMA_MODEL=llava:latest
# Request timeout in milliseconds (default: 300000 = 5 minutes)
OLLAMA_TIMEOUT=300000

# =============================================================================
# AUTHENTICATION CONFIGURATION
# =============================================================================

# Session secret for secure sessions (change in production)
SESSION_SECRET=your-super-secret-session-key-change-in-production

# No additional authentication configuration needed
# The application uses username/password authentication with sessions

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Server port (default: 3001)
PORT=3001

# Environment (development, production)
NODE_ENV=development

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# SQLite database file path
DATABASE_PATH=./database.sqlite

# =============================================================================
# FILE UPLOAD CONFIGURATION
# =============================================================================

# Upload directory for original images
UPLOAD_DIR=./uploads

# Thumbnail directory for generated previews
THUMBNAIL_DIR=./thumbnails

# Maximum file size in bytes (50MB = 52428800)
MAX_FILE_SIZE=52428800

# Supported image formats (automatically detected)
SUPPORTED_FORMATS=jpg,jpeg,png,tiff,tif,cr2,nef,arw,dng,raf,orf,rw2

# =============================================================================
# IMAGE PROCESSING CONFIGURATION
# =============================================================================

# Default thumbnail size in pixels (100-800, default: 300)
THUMBNAIL_SIZE=300

# Image size for AI analysis in pixels (512-2048, default: 1024)
# This applies to both Gemini and Ollama providers
AI_IMAGE_SIZE=1024

# Legacy: GEMINI_IMAGE_SIZE is still supported for backward compatibility
# GEMINI_IMAGE_SIZE=1024

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Frontend development server URL (for CORS)
CLIENT_URL=http://localhost:5173

# Enable debug logging (uncomment to enable)
# DEBUG=true

# Image processing quality (1-100, default: 85)
# IMAGE_QUALITY=85
